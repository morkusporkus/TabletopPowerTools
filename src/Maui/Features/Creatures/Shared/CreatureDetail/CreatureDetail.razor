
<MudGrid>
    <MudItem xs="5">
        <MudText Typo="Typo.h4">@_response.Creature.Name</MudText>
    </MudItem>
    <MudItem xs="2">
    <MudText Typo="Typo.h5">@_response.Creature.Alignment</MudText>
      </MudItem>
     <MudItem xs="2">
    <MudText Typo="Typo.h5">AC:@_response.Creature.ArmorClass</MudText>
      </MudItem>
    <MudItem xs="12">
        @foreach (var ability in _response.Creature.Abilities)
        {
            <MudText>@ability.Name</MudText>
            @MarkdownDisplay(ability.Desc)
        }
        @foreach (var action in _response.Creature.Actions)
        {
            <MudText>@action.Name</MudText>
            @MarkdownDisplay(action.Desc)
        }
        <MudText Typo="Typo.h6">Attributes</MudText>
        <MudGrid>
            <AttributeScore Name="STR" RawPoints="@_response.Creature.StrPoints" />
            <AttributeScore Name="DEX" RawPoints="@_response.Creature.DexPoints" />
            <AttributeScore Name="CON" RawPoints="@_response.Creature.ConPoints" />
            <AttributeScore Name="WIS" RawPoints="@_response.Creature.WisPoints" />
            <AttributeScore Name="INT" RawPoints="@_response.Creature.IntPoints" />
            <AttributeScore Name="CHA" RawPoints="@_response.Creature.ChaPoints" />
        </MudGrid>
    </MudItem>
</MudGrid>
